<ul class="list-unstyled">
  <% favorites.each do |favorite| %>
    <li class="media mb-3">
      <img class="mr-2 rounded" src="<%= gravatar_url(favorite.user, { size: 50 }) %>" alt="">
      <div class="media-body">
        <div>
          <%= link_to favorite.micropost.name, user_path(favorite.user) %> <span class="text-muted">posted at <%= favorite.created_at %></span>
        </div>
        <div>
          <p class="mb-0"><%= favorite%></p>
        </div>
        <div>
          <% if current_user == favorite.user %>
            <%= link_to "Delete", favorite, method: :delete, data: { confirm: "You sure?" }, class: 'btn btn-danger btn-sm' %>
          <% end %>
          
          <%= render 'favorites/favorite_button',user: favorite%>
          
        </div>
      </div>
    </li>
  <% end %>
  <%= paginate favorites %>
</ul>